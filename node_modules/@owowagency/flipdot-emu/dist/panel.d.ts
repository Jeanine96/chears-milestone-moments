export declare class Panel {
    readonly address: number;
    readonly columns: Uint8Array;
    readonly command: Uint8Array;
    dirty: boolean;
    constructor(address: number, width: number);
    createCommand(flush: boolean): Uint8Array<ArrayBufferLike>;
    getPixel(x: number, y: number): boolean;
    setPixel(x: number, y: number, on: boolean): void;
    putColumn(index: number, data: number): void;
    markFlushed(): void;
}
