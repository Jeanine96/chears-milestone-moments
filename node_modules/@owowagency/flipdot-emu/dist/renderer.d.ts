export declare class TerminalRenderer {
    width: number;
    height: number;
    private out;
    private previous;
    constructor(width: number, height: number);
    getLuminanceRGB(r: number, g: number, b: number): 0 | 1;
    private getPixel;
    render(pixels: Uint8Array): void;
    private write;
    private fill;
    start(): void;
}
