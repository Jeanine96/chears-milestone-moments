import type { TransportOptions } from "./types";
export interface Transport {
    send(data: Uint8Array): boolean;
    connect(): Promise<void>;
}
export declare function create(options: TransportOptions): Transport;
